// Placeholder root so Next has a page to build. The real surface is the MCP
// endpoint at /m/<secret>/mcp.
export default function Page() {
  return <main style={{ fontFamily: 'system-ui', padding: 32 }}>PDS GL MCP server.</main>;
}
